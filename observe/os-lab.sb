;; macOS Seatbelt 沙箱配置（任务 5 第 3 步）。这个文件由你补完。
;; 角色：让 Server 进程在操作系统层被拒绝读诱饵目录，与 check.ts 的应用层校验对照。
;; observe.ts 运行前把 __SECRET_DIR__ 换成 .os-lab.json 里 decoy 所在的目录，
;; 写到 runs/<os>/task5-sandbox/os-lab.sb；driver 用 sandbox-exec -f <该文件> 启动 Server。
;; 已给：默认全放行。要补：一条规则，拒绝对 __SECRET_DIR__ 及其子路径的一切读操作。
;; 提示：Seatbelt 的操作名 file-read* 覆盖 open/stat 等读类操作，路径匹配用 (subpath "...")。
;; 参考：/System/Library/Sandbox/Profiles/ 下的 .sb 文件，`man sandbox-exec`。
(version 1)
(allow default)
;; TODO：在这里加一条 deny 规则
