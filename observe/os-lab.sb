;; macOS Seatbelt 沙箱模板（任务 5 第 3 步）。
;; 在实验里的角色：让 Server 进程在 OS 层被拒绝读诱饵目录，与 check.ts 的应用层校验对照。
;; observe.ts 运行前把占位符换成 .os-lab.json 里 decoy 所在目录，写到 runs/<os>/task5-sandbox/os-lab.sb；
;; driver 用 sandbox-exec -f <该文件> 启动 Server。默认全放行，只拒绝该目录下的一切读操作。
(version 1)
(allow default)
(deny file-read* (subpath "__SECRET_DIR__"))
