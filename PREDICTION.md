# 预测在跑任务 2 之前写，与 DOCTOR.md 同一个 commit

先读 `实验指导书.html` 的第二节实验原理，再填本文件。「预测」在课前填，「实测」与「差异原因」在课内跑完任务 2 后填，并注明来自哪个证据文件的第几行。预测错了不扣分，对预测失误的解释引用本人日志才得分。这个 commit 必须早于 `runs/<os>/task2/` 的首次提交。

姓名：　学号：　GitHub 用户名：　系统（macos 或 linux）：

## 任务 2 跑 npm run observe -- stdio 时会发生什么，四项各写一句

1. 一次运行涉及几个本机进程（驱动程序、tee、Server 之外还有吗）。
   预测：　实测（文件:行号）：　差异原因：
2. 父子关系是什么（Server 的父进程是谁，tee 的父进程是谁）。
   预测：　实测（文件:行号）：　差异原因：
3. 目标文件的 open 由哪个进程发出（驱动程序、tee 还是 Server）。
   预测：　实测（文件:行号）：　差异原因：
4. 密钥 DEEPSEEK_API_KEY 会不会出现在 Server 子进程的环境变量里，`events.log` 里能不能看到。
   预测：　实测（文件:行号）：　差异原因：
